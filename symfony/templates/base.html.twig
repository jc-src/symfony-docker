<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Hello Bulma!</title>
    <link rel="stylesheet" href="/bulma/css/bulma.min.css">
</head>
<body>

{% for message in app.flashes('success') %}
    <div class="notification is-success">{{ message }}</div>
{% endfor %}

<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item" href="https://bulma.io">
            <img src="https://bulma.io/images/bulma-logo.png" width="112" height="28">
        </a>
    </div>

    <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
            <a class="navbar-item" href="{{ path('app_home') }}">Home</a>
            <a class="navbar-item" href="https://bulma.io/documentation/">Documentation</a>
        </div>
        <div class="navbar-end">
        {% if app.user %}
            <div class="navbar-item">
                <p>
                    You are logged in as:
                    <a href="{{ path('app_secure_home') }}">{{ app.user.username }}</a>
                </p>
            </div>
            <div class="navbar-item">
                <div class="buttons">
                    <a class="button is-light" href="{{ path('app_logout') }}">Logout</a>
                </div>
            </div>
        {% else %}
            <div class="navbar-item">
                <div class="buttons">
                    <a class="button is-light" href="{{ path('app_secure_home') }}">Log in</a>
                </div>
            </div>
        {% endif %}
    </div>
  </div>
</nav>

<section class="hero is-fullheight">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">{% block title %}Hello World{% endblock %}</h1>
            <p class="subtitle">{% block subtitle %}Welcome to Demo page!{% endblock %}</p>
            {% block body %}{% endblock %}
        </div>
    </div>
</div>

</body>
</html>
